import React from "react";
import Card from "react-bootstrap/Card";

function Footer() {
  return (
    <div>
      <Card className="text-center">
        <Card.Footer className="text-muted">Our App</Card.Footer>
      </Card>
    </div>
  );
}

export default Footer;
